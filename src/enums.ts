export enum USER_AVATAR_SIZE {
    S = 36,
    M = 50,
    L = 72
}

export enum BUTTON_SIZE {
    S = 'button-size_S',
    M = 'button-size_M',
    L = 'button-size_L'
}

export enum ARTICLE_NODE_TYPE {
    heading = 'heading',
    paragraph = 'paragraph',
    image = 'image'
}