import React from 'react';
import styles from './ArticlesSwiper.module.scss';

export const ArticlesSwiper: React.FC = (): React.ReactElement | null => {

    return (
        <div className={styles.swiper}></div>
    );
}